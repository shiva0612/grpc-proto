generating gocode from .proto files using protoc cmd:
flags used:

--proto-path = mention the path for proto files
--go_out = output dir for models
--go-grpc_out = out dir for grpc services
--go_opt = options for models generated
--go-grpc_opt = options for services generated
